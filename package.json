{
  "name": "spmb-v2",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mkdir -p backend/public && cp -r frontend/dist/. backend/public/ && npm install --prefix backend --omit=dev",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
